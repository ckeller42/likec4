---
'@likec4/language-server': minor
'@likec4/mcp': minor
'@likec4/config': patch
---

Add new MCP query tools for graph traversal and filtering: `query-graph`, `query-incomers-graph`, `query-outgoers-graph`, `query-by-metadata`, `query-by-tags`, and `find-relationship-paths`.

Enhance `read-project-summary` to include serialized project `config` and extend project config schema with `metadata`.
